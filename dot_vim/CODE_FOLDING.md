# 代码折叠快速参考

在 VSCode Vim、GoLand IdeaVim 和 Neovim 中统一的代码折叠快捷键。

---

## 📚 基础折叠操作

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `za` | 切换折叠 | 折叠 ↔ 展开 (最常用) |
| `zc` | 折叠 | 关闭当前代码块 |
| `zo` | 展开 | 打开当前代码块 |
| `zC` | 递归折叠 | 折叠当前块及所有子块 |
| `zO` | 递归展开 | 展开当前块及所有子块 |

**记忆技巧:**
- **z** - 折叠相关操作都以 `z` 开头
- **a** - toggle (**a**lternate)
- **c** - **c**lose (折叠)
- **o** - **o**pen (展开)
- **大写** = 递归操作

---

## 🌍 全局折叠操作

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `zM` | 折叠所有 | 折叠文件中所有代码块 |
| `zR` | 展开所有 | 展开文件中所有代码块 |

**记忆技巧:**
- **M** - **M**ore fold (更多折叠)
- **R** - **R**educe fold (减少折叠)

---

## 🔍 折叠导航

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `zj` | 下一个折叠 | 跳转到下一个折叠区域 |
| `zk` | 上一个折叠 | 跳转到上一个折叠区域 |

**记忆技巧:**
- **j** - 向下 (Vim 标准)
- **k** - 向上 (Vim 标准)

---

## 💡 实际使用示例

### 场景 1: 折叠函数体

**代码:**
```go
func calculateTotal(items []Item) float64 {
    total := 0.0
    for _, item := range items {
        total += item.Price
    }
    return total
}
```

**操作:**
1. 光标移到函数内任意位置
2. 按 `zc` → 折叠整个函数
3. 按 `zo` → 展开函数

**结果:**
```go
func calculateTotal(items []Item) float64 {▶ }
```

---

### 场景 2: 快速浏览代码结构

**操作流程:**
1. 打开一个大文件
2. 按 `zM` → 折叠所有代码块
3. 查看文件结构(只看函数签名)
4. 移到感兴趣的函数
5. 按 `zo` → 展开查看详情
6. 按 `zR` → 需要时展开所有

**示例:**
```go
// 折叠所有后 (zM)
type User struct {▶ }
func NewUser() *User {▶ }
func (u *User) Validate() error {▶ }
func (u *User) Save() error {▶ }

// 展开某个函数 (zo)
type User struct {▶ }
func NewUser() *User {
    return &User{
        ID: uuid.New(),
        CreatedAt: time.Now(),
    }
}
func (u *User) Validate() error {▶ }
func (u *User) Save() error {▶ }
```

---

### 场景 3: 嵌套代码折叠

**代码:**
```go
type Service struct {
    repo Repository
}

func (s *Service) ProcessOrder(order Order) error {
    if err := order.Validate(); err != nil {
        return err
    }

    for _, item := range order.Items {
        if item.Stock < item.Quantity {
            return ErrInsufficientStock
        }
    }

    return s.repo.Save(order)
}
```

**操作:**
1. 光标在 `ProcessOrder` 函数内
2. `zc` → 折叠整个函数
3. `zo` → 展开函数
4. 光标移到 `if err := order.Validate()` 块
5. `zc` → 折叠 if 块
6. `zC` → 递归折叠(折叠所有嵌套块)

---

### 场景 4: 在代码审查时使用

**操作流程:**
1. 打开要审查的文件
2. `zM` → 折叠所有
3. `zj` → 跳到下一个函数
4. `za` → 切换折叠查看函数内容
5. 审查完毕后 `za` → 折叠
6. `zj` → 继续下一个函数
7. 重复步骤 4-6

---

## 🎯 常用工作流

### 工作流 1: 专注当前函数

```vim
1. zM     " 折叠所有
2. zo     " 展开当前函数
3. 编辑...
4. zR     " 完成后展开所有
```

### 工作流 2: 快速查找函数

```vim
1. zM           " 折叠所有
2. /functionName " 搜索函数名
3. zo           " 展开找到的函数
4. 查看/编辑...
5. zc           " 折叠并继续搜索
6. n            " 下一个匹配
7. zo           " 展开
```

### 工作流 3: 对比两个函数

```vim
1. zM     " 折叠所有
2. 定位到函数1
3. zo     " 展开函数1
4. zj     " 跳到下一个函数
5. zo     " 展开函数2
6. Ctrl-w v  " 垂直分屏对比
```

---

## 📊 折叠级别对比

| 级别 | 命令 | 效果 | 适用场景 |
|------|------|------|----------|
| 完全折叠 | `zM` | 所有代码块折叠 | 查看文件结构 |
| 折叠当前 | `zc` | 当前块折叠 | 隐藏当前函数 |
| 展开当前 | `zo` | 当前块展开 | 查看函数详情 |
| 完全展开 | `zR` | 所有代码块展开 | 正常编辑 |

---

## 🔧 编辑器特定说明

### VSCode
- ✅ 所有折叠命令已配置并可用
- 💡 折叠图标会显示在行号旁边
- 💡 鼠标悬停可预览折叠内容

### GoLand / IntelliJ IDEA
- ✅ 所有折叠命令已配置并可用
- 💡 支持更多折叠类型(导入、注释等)
- 💡 可以在 Settings → Editor → Code Folding 自定义

### Neovim
- ✅ Vim 原生支持,无需配置
- 💡 可以设置 `foldmethod`:
  ```vim
  set foldmethod=indent  " 基于缩进
  set foldmethod=syntax  " 基于语法
  set foldmethod=marker  " 基于标记
  ```

---

## 💭 小技巧

### 1. 结合搜索使用
```vim
/function     " 搜索 function
zM            " 折叠所有
n             " 下一个匹配
zo            " 展开查看
```

### 2. 快速审查代码
```vim
zM            " 折叠所有,查看大纲
zj zj zj      " 快速跳转函数
za            " 切换查看
```

### 3. 专注编辑
```vim
zM            " 折叠所有
zo            " 展开当前编辑区域
" 现在可以专注于当前函数,不被其他代码干扰
```

### 4. 保存折叠状态 (Neovim)
```vim
:mkview       " 保存当前视图(包括折叠)
:loadview     " 恢复视图
```

---

## ⚠️ 注意事项

1. **VSCode:** 某些语言可能需要安装对应的语言扩展才能正确折叠
2. **GoLand:** 默认会自动折叠导入语句,可以在设置中调整
3. **大文件:** 折叠大量代码块可能影响性能,建议按需折叠

---

## 🎓 记忆口诀

**基础操作:**
- "Za 切换最方便"
- "Zc 关门 Zo 开门"
- "大写递归别忘记"

**全局操作:**
- "ZM 全关 ZR 全开"
- "Zj 向下 Zk 向上"

**使用建议:**
- "看结构用 zM"
- "看细节用 zo"
- "来回切就用 za"

---

## 📚 相关资源

- Vim 折叠文档: `:help fold`
- VSCode 折叠: Cmd+K Cmd+J (切换所有折叠)
- GoLand 折叠: Cmd+- / Cmd+= (折叠/展开)

---

## ✅ 快速测试清单

在任意代码文件中测试:

- [ ] `za` - 切换折叠
- [ ] `zc` - 折叠当前块
- [ ] `zo` - 展开当前块
- [ ] `zM` - 折叠所有
- [ ] `zR` - 展开所有
- [ ] `zj` - 跳到下一个折叠
- [ ] `zk` - 跳到上一个折叠

全部成功 = 配置正确! 🎉
